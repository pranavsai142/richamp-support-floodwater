# Meshes — index only

After parse, **read only the matching card** (not all three):

| User / default | Load |
|----------------|------|
| `ricv1` / skill / obs / station | `mesh-ricv1.md` |
| `v18` | `mesh-v18.md` |
| `ec95d` / smoke | `mesh-ec95d.md` |
| unspecified | prefer **ricv1** if drive has floodwater linked_files; else ec95d local |

Do not mix fort.13/14/15 across rows. Shared pipeline stays in parent `SKILL.md`.
